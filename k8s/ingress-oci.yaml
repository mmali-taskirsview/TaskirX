apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: taskir-ingress-oci
  namespace: taskir
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/use-regex: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  ingressClassName: nginx
  tls:
  - hosts:
    - taskirx.com
    - www.taskirx.com
    - dashboard.taskirx.com
    - api.taskirx.com
    - bidding.taskirx.com
    secretName: taskir-tls-secret
  rules:
  - host: taskirx.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: next-dashboard
            port:
              number: 3001
  - host: www.taskirx.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: next-dashboard
            port:
              number: 3001
  - host: dashboard.taskirx.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: next-dashboard
            port:
              number: 3001
  - host: api.taskirx.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: nestjs-backend
            port:
              number: 3000
  - host: bidding.taskirx.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: go-bidding
            port:
              number: 8080
